package io.dane.validator.provider;

import io.dane.validator.data.HRData;
import io.dane.validator.domain.Department;

public class HRProvider {

    public Department findDepartmentByCode(String code) {
        var data = HRData.getInstance();

        return null;
    }
}
